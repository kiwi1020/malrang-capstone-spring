spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${malrang-service.security.oauth2.client.registration.google.client-id:${GOOGLE_CLIENT_ID}}
            client-secret: ${malrang-service.security.oauth2.client.registration.google.client-secret:${GOOGLE_CLIENT_SECRET}}
            scope:
              - profile
              - email